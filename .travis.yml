language: python
python:
  - "3.6"
  - "3.8"
script:
  - python saxo -v
  - python saxo test
  - pip install flake8
  - flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
notifications:
  irc:
    channels:
      "chat.freenode.net#swhack"
    on_success: change
    skip_join: true
